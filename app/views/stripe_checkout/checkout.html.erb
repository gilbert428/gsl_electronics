<!-- app/views/stripe_checkout/checkout.html.erb -->
<div class="container mx-auto p-4" data-controller="stripe-checkout" data-stripe-checkout-public-key-value="<%= Rails.application.credentials.dig(:stripe, :pk) %>" data-stripe-checkout-session-id-value="<%= @session_id %>">
  <h1 class="text-2xl font-bold mb-4">Complete Your Purchase</h1>
  <p>Redirecting to Stripe...</p>
</div>
